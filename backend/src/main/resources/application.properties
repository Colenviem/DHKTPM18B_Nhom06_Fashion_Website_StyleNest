# ===============================
# ? C?u hình ?ng d?ng
# ===============================
spring.application.name=stylenet
server.port=8080

# ===============================
# ? K?t n?i MongoDB
# ===============================
# Cách 1: n?u MongoDB ch?y local m?c ??nh (port 27017)
spring.data.mongodb.uri=mongodb://localhost:27017/stylenet_db

# Cách 2 (tu? ch?n): n?u b?n dùng MongoDB Atlas (cloud)
# spring.data.mongodb.uri=mongodb+srv://<username>:<password>@<cluster-url>/user_db?retryWrites=true&w=majority

# ===============================
# ?? C?u hình khác
# ===============================
spring.data.mongodb.database=stylenet_db

# Ghi log truy v?n MongoDB ra console (giúp debug d? h?n)
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.mongodb.driver=INFO

# Cho phép frontend (React, Angular, v.v.) truy c?p API (n?u c?n)
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS

# ===============================
# ? ??nh d?ng th?i gian m?c ??nh (ISO 8601)
# ===============================
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss.SSS'Z'
spring.jackson.time-zone=Asia/Ho_Chi_Minh
