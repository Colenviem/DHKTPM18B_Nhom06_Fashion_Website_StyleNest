server:
  port: ${PORT:8080}

# ==============================
# Custom App Configuration
# ==============================
app:
  # Nếu có biến môi trường FRONTEND_URL thì dùng, nếu không thì mặc định là localhost
  frontend-url: ${FRONTEND_URL:http://localhost:5173}
  brevo:
    api-key: ${BREVO_API_KEY}
    sender-email: "tranngochung19112004@gmail.com"
    sender-name: "StyleNest"

sepay:
  # Cấu hình từ snippet của bạn
  api-key: ${SEPAY_API_KEY:VEVQTLYOZLWJXJ0NGHUMPI84UBLDZS6TEPKUWCDOKSJFGEMKDV7X9WCNSEQP2IO6}
  client-key: ${SEPAY_CLIENT_KEY:VEVQTLYOZLWJXJ0NGHUMPI84UBLDZS6TEPKUWCDOKSJFGEMKDV7X9WCNSEQP2IO6}
  env: ${SEPAY_ENV:production}
  qr-url: https://qr.sepay.vn/img
  webhook-url: ${SEPAY_WEBHOOK_URL:https://dhktpm18b-nhom06-fashion-website.onrender.com/api/payments/sepay/webhook}
  transaction-timeout: ${TRANSACTION_TIMEOUT:300}
  client-urls: ${CLIENT_URL:https://style-nest-frontend-n5wt0aus7.vercel.app,http://localhost:3000}

  # [QUAN TRỌNG] Cần bổ sung 2 dòng này để code Java tạo được QR Code
  bank-account: ${SEPAY_BANK_ACCOUNT:0326829327}   # Ví dụ: 0368... (Số tài khoản nhận tiền)
  bank-code: ${SEPAY_BANK_CODE:MB}         # Ví dụ: MB, VCB (Mã ngân hàng)
spring:
  application:
    name: spring-ai
  data:
    mongodb:
      uri: ${MONGODB_URI}

  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

  # Cấu hình RabbitMQ
  rabbitmq:
    uri: ${RABBITMQ_URI}

  # Cấu hình Spring AI
  ai:
    openai:
      api-key: ${GEMINI_API_KEY}
      chat:
        base-url: https://generativelanguage.googleapis.com
        completions-path: /v1beta/openai/chat/completions
        options:
          model: gemini-2.0-flash

# JWT configuration
jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000

rabbitmq:
  exchange: notification.exchange
  routing-key: notification.routing.key

# Cloudinary Config
cloudinary:
  cloud_name: ${CLOUDINARY_CLOUD_NAME}
  api_key: ${CLOUDINARY_API_KEY}
  api_secret: ${CLOUDINARY_API_SECRET}

# Logging levels
logging:
  level:
    org:
      springframework:
        mail: DEBUG
        amqp: DEBUG
      eclipse:
        angus:
          mail: DEBUG
    com:
      rabbitmq: DEBUG
